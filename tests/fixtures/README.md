# Test fixtures

Prompt 0 uses generated deterministic data and therefore needs no stored fixture.
Later milestones may add secret-free structured fixtures only when their binding
acceptance criteria require them.

